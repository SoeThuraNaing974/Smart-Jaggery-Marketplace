# Frontend (Express) environment — copy to .env for local dev.
PORT=3000
API_BASE=http://127.0.0.1:5000
# JWT_SECRET MUST be identical to the backend's JWT_SECRET:
JWT_SECRET=change-me-to-a-long-random-string
# In production hosting set: NODE_ENV=production
