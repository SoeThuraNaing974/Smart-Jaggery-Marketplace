# Backend (Flask API) environment — copy to .env for local dev.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/jaggery_db
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXP_HOURS=12
UPLOAD_FOLDER=uploads
# Optional real email (leave blank for dry-run):
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASS=
